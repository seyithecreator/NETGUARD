services:
  - type: web
    name: netguard-ids
    env: python
    pythonVersion: "3.11.9"
    buildCommand: pip install -r requirements.txt && python train.py || echo "Training failed — will train at runtime"
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --timeout 120
